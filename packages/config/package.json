{
  "name": "@twin/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript configuration for the digital-twin workspace.",
  "files": [
    "tsconfig.base.json",
    "tsconfig.react.json",
    "tsconfig.node.json"
  ],
  "scripts": {
    "typecheck": "echo 'no sources'",
    "test": "echo 'no tests'"
  }
}
