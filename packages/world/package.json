{
  "name": "@twin/world",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-free motion model for the Living Survey: skeleton rigs, deterministic wind, bounded deformation.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "build": "echo 'world: consumed from source'"
  },
  "devDependencies": {
    "@twin/config": "workspace:*",
    "@types/node": "^26.5.1",
    "typescript": "~6.0.3",
    "vitest": "^5.0.0"
  }
}
